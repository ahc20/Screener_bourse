# CAC 40 Screener OBV

Screener CAC 40 avec analyse multi-indicateurs (synthese + OBV + MM + zones d entree optimales).

Mis a jour automatiquement chaque jour a 4h du matin via cron local + push GitHub. Vercel redeploie automatiquement.

Donnees Yahoo Finance, calculs locaux, zero appel API en production.
